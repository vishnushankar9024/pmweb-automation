
@section Scripts
{
    <script>
        $(function () {
            var requestOptions = {
                method: 'GET',
                redirect: 'follow'
            };

            var urlParams = new URLSearchParams(window.location.search);
            var dat = "http://localhost:81?ProjectId=" + urlParams.get('ProjectId') + "&RecordId=" + urlParams.get('RecordId');
            document.getElementById('IFrameMultiBid').src = dat

            //fetch("https://cmcs.pmweb.com/7_0_00/PMWeb/Custom/PmwebHelper.aspx/GetUser?_=" + new Date().getTime(), requestOptions)
            //    .then(response => {
            //        var testData = { "d": "{ \"Id\":\"5\",\"Email\":\"pmweb@cmcs-mena.com\",\"Username\":\"admin\"}" };
            //        var abcd = JSON.parse(response.d)

            //        var urlParams = new URLSearchParams(window.location.search);
            //        var dat = "http://localhost:81?id=" + abcd.Id + "&ProjectId=" + urlParams.get('ProjectId') + "&RecordId=" + urlParams.get('RecordId');
            //        alert(dat);
            //        document.getElementById('IFrameMultiBid').src = dat
            //    })
            //    .then(result => console.log(result))
            //    .catch(error => console.log('error', error));

        })
    </script>
}
<div>
    <iframe id="IFrameMultiBid" frameborder="0" width="100%" height="530px"></iframe>
</div>
